{
  "name": "pasadena-party-co-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded sass/main.scss:styles/styles.css",
    "sass-prod": "sass --no-source-map --style=compressed sass/main.scss:styles/styles.css",
    "prefix": "postcss --use autoprefixer -b 'last 10 versions' styles/styles.css -o style.prefix.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.4.12",
    "sass": "^1.54.9"
  },
  "devDependencies": {
    "postcss": "^8.4.16",
    "postcss-cli": "^10.0.0"
  }
}
